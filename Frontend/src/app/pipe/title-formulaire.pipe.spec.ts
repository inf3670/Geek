import { TitleFormulairePipe } from './title-formulaire.pipe';

describe('TitleFormulairePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFormulairePipe();
    expect(pipe).toBeTruthy();
  });
});
